# 🏗️ NetChi - System Architecture

Complete system architecture and data flow documentation.

## Overall Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    USER'S BROWSER                            │
│              (React 19 + Vite 6 Frontend)                    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ http://localhost:5173                               │    │
│  │ - React Components                                  │    │
│  │ - Framer Motion Animations                          │    │
│  │ - Tailwind CSS Styling                              │    │
│  │ - Offline CSS & Fonts                               │    │
│  └─────────────────────────────────────────────────────┘    │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ├─── HTTP/REST ──────────┐
                       │                        │
                       ├─── WebSocket/SignalR ─┐│
                       │                       ││
                       ▼                       ▼▼
          ┌────────────────────────────────────────────────┐
          │   ASP.NET Core 10 - API Server                │
          │    (https://localhost:5001)                   │
          │                                                │
          │ ┌──────────────────────────────────────────┐  │
          │ │ NetChi.Api                                │  │
          │ │ - Controllers (REST endpoints)           │  │
          │ │ - OrderHub (SignalR real-time)           │  │
          │ │ - Security & Auth middleware             │  │
          │ │ - CORS, Rate Limiting, Headers           │  │
          │ │ - Swagger UI (/health endpoint)          │  │
          │ └──────────────────────────────────────────┘  │
          │                                                │
          │ ┌──────────────────────────────────────────┐  │
          │ │ Clean Architecture Layers                │  │
          │ │ - Application (DTOs, Mapping)            │  │
          │ │ - Domain (Entities, Business rules)      │  │
          │ │ - Infrastructure (DbContext, Repos)      │  │
          │ │ - Shared (Constants, Helpers)            │  │
          │ └──────────────────────────────────────────┘  │
          └───────────────────┬────────────────────────────┘
                              │
                    ┌─────────┴─────────┐
                    │                   │
                    ▼                   ▼
        ┌───────────────────┐   ┌──────────────────┐
        │  SQL Server 2022  │   │ File System      │
        │                   │   │ - Offline CSS    │
        │ Database:         │   │ - Font Assets    │
        │ - NetChiDb        │   │ - Static Files   │
        │ - Users table     │   │                  │
        │ - Indexes         │   │ public/           │
        │ - Relationships   │   │ ├── css/         │
        │                   │   │ ├── fonts/       │
        │ (Docker running)  │   │ └── ...          │
        └───────────────────┘   └──────────────────┘
```

---

## Frontend Architecture

### React Component Tree
```
App.tsx
├── Header
│   ├── Navigation
│   └── UserMenu
├── Main Content
│   ├── HeroSection
│   ├── GeminiChat
│   ├── DesktopLayout
│   ├── OrdersPage (future)
│   └── ...
└── Footer
    └── Links
```

### Services Layer
```
Services/
├── gemini.ts (Google GenAI)
├── apiClient.ts (HTTP)
│   ├── ApiClient.login()
│   ├── ApiClient.get()
│   ├── ApiClient.post()
│   ├── ApiClient.put()
│   └── ApiClient.delete()
├── signalRClient.ts (Real-time)
│   ├── SignalRClient.connect()
│   ├── SignalRClient.sendOrderUpdate()
│   ├── SignalRClient.joinOrderGroup()
│   └── SignalRClient.onOrderStatusChanged()
└── apiConfig.ts (Configuration)
    ├── Development config
    └── Production config
```

### React Hooks
```
Hooks/
├── useApi.ts
│   ├── useApi() - Generic hook
│   ├── useLogin() - Authentication
│   └── useOrders() - Orders management
└── useSignalR.ts
    └── useSignalR() - Real-time updates
```

### State Management Flow
```
┌─────────────────────────────────────┐
│  User Action (click, input, etc)    │
└────────────────┬────────────────────┘
                 │
                 ▼
    ┌─────────────────────────┐
    │  React Event Handler    │
    │  (onClick, onChange)    │
    └────────┬────────────────┘
             │
             ▼
    ┌──────────────────────────────┐
    │  Hook Function Called        │
    │  (useApi, useSignalR)        │
    └────────┬─────────────────────┘
             │
             ▼
    ┌──────────────────────────────┐
    │  Call Service               │
    │  (ApiClient, SignalRClient) │
    └────────┬─────────────────────┘
             │
             ▼
    ┌──────────────────────────────┐
    │  HTTP or WebSocket Request  │
    └────────┬─────────────────────┘
             │
             ▼
    ┌──────────────────────────────┐
    │  Backend Processing         │
    └────────┬─────────────────────┘
             │
             ▼
    ┌──────────────────────────────┐
    │  Response to Client          │
    └────────┬─────────────────────┘
             │
             ▼
    ┌──────────────────────────────┐
    │  Update Component State      │
    │  (setState, loading, data)   │
    └────────┬─────────────────────┘
             │
             ▼
    ┌──────────────────────────────┐
    │  React Re-render UI          │
    │  (Show new data)             │
    └──────────────────────────────┘
```

---

## Backend Architecture

### Clean Architecture Layers
```
┌─────────────────────────────────────────────────────┐
│               NetChi.Api (Presentation)             │
│         Web API Layer - Controllers & Hubs          │
│  - OrdersController (REST endpoints)                │
│  - OrderHub (Real-time SignalR)                     │
│  - Security middleware                              │
└─────────────────────────────────────────────────────┘
                         │
                         │ depends on
                         ▼
┌─────────────────────────────────────────────────────┐
│           NetChi.Application (Business Logic)       │
│     - Use Cases / Commands / Queries (via CQRS)    │
│     - DTOs (Data Transfer Objects)                  │
│     - AutoMapper Profiles                           │
│     - Interfaces                                    │
│     - Validations                                   │
└─────────────────────────────────────────────────────┘
                         │
                         │ depends on
                         ▼
┌─────────────────────────────────────────────────────┐
│              NetChi.Infrastructure (Data Access)    │
│     - DbContext (Entity Framework Core)             │
│     - Repository Implementations                    │
│     - External Service Integrations                 │
│     - Authentication Services                       │
└─────────────────────────────────────────────────────┘
                         │
                         │ depends on
                         ▼
┌─────────────────────────────────────────────────────┐
│                NetChi.Domain (Core Logic)           │
│          - Entities (User, Order, etc)              │
│          - Value Objects                            │
│          - Enumerations (UserType)                  │
│          - Domain Interfaces                        │
│          - Business Rules                           │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│               NetChi.Shared (Cross-cutting)         │
│  - Constants                                        │
│  - Helper Functions                                 │
│  - Extensions                                       │
│  - Resource Files                                   │
└─────────────────────────────────────────────────────┘
```

### Request Flow
```
Client (HTTP Request)
    │
    │ GET /api/v1/orders
    │
    ▼
┌─────────────────────────────────────┐
│ Middleware Pipeline                 │
├─────────────────────────────────────┤
│ 1. HTTPS Redirect                   │
│ 2. Rate Limiter (100 req/min)       │
│ 3. CORS Handler                     │
│ 4. Security Headers                 │
│ 5. Authentication (JWT)             │
│ 6. Authorization                    │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│ Routing                             │
│ Matched to: OrdersController        │
│ Action: GetOrders()                 │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│ Model Validation                    │
│ (FluentValidation)                  │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│ Application Layer                   │
│ - Use Case Logic                    │
│ - Data Transformation (AutoMapper)  │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│ Infrastructure Layer                │
│ - DbContext Query                   │
│ - Repository Access                 │
│ - Database (SQL Server)             │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│ Response Building                   │
│ - Status Code 200 OK                │
│ - JSON serialization                │
│ - Compression (gzip)                │
└─────────────────────────────────────┘
    │
    ▼
Client (JSON Response)
```

---

## Real-time (SignalR) Architecture

### Connection Flow
```
Client                              Server
   │                                 │
   │ 1. Connect to /hubs/order      │
   │────────────────────────────────>│
   │    with JWT token              │
   │                                 │
   │ 2. WebSocket established       │
   │<────────────────────────────────│
   │    "Connected" event            │
   │                                 │
   │ 3. JoinOrderGroup("order-123") │
   │────────────────────────────────>│
   │                                 │
   │                 ┌───────────────┤
   │                 │ User added to │
   │                 │ order-123     │
   │                 │ group         │
   │                 └───────────────┤
   │                                 │
   │ 4. SendOrderStatusUpdate       │
   │────────────────────────────────>│
   │    ("order-123", "completed")  │
   │                                 │
   │                 ┌───────────────┤
   │                 │ Broadcast to  │
   │                 │ order-123     │
   │                 │ group         │
   │                 └───────────────┤
   │                                 │
   │ 5. OrderStatusChanged event    │
   │<────────────────────────────────│
   │    {orderId, status, ...}       │
   │                                 │
   │ 6. Disconnect (Optional)       │
   │────────────────────────────────>│
   │    LeaveOrderGroup, Disconnect │
```

### Hub Methods
```
OrderHub
├── Client → Server (Invocations)
│   ├── SendOrderUpdate(message)
│   ├── JoinOrderGroup(orderId)
│   ├── LeaveOrderGroup(orderId)
│   └── SendOrderStatusUpdate(orderId, status)
│
└── Server → Client (Events)
    ├── Connected(connectionInfo)
    ├── ReceiveOrderUpdate(message)
    ├── OrderStatusChanged(data)
    ├── UserJoined(message)
    ├── UserLeft(message)
    └── UserDisconnected(connectionInfo)
```

---

## Data Flow: Complete User Journey

### 1. Authentication Journey
```
┌─────────────┐
│ Login Page  │ User enters email/password
└──────┬──────┘
       │
       ▼
┌──────────────────────┐
│ useLogin() hook      │
│ ApiClient.login()    │
└──────┬───────────────┘
       │ HTTP POST /api/auth/login
       ▼
┌──────────────────────┐
│ Backend              │
│ Validate credentials │
│ Generate JWT token   │
└──────┬───────────────┘
       │ Return {token, user}
       ▼
┌──────────────────────┐
│ Frontend             │
│ Save token to        │
│ localStorage         │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Redirect to Orders   │
│ Token in requests    │
└──────────────────────┘
```

### 2. Order View Journey
```
┌──────────────┐
│ Orders Page  │ Component mounts
└──────┬───────┘
       │
       ▼
┌──────────────────────────┐
│ useOrders().getOrders()  │
└──────┬───────────────────┘
       │ HTTP GET /api/v1/orders
       │ + Authorization: Bearer {token}
       ▼
┌──────────────────────────┐
│ Backend                  │
│ - Verify JWT token       │
│ - Check authorization    │
│ - Query database         │
│ - Return order list      │
└──────┬───────────────────┘
       │ JSON response
       ▼
┌──────────────────────────┐
│ Frontend                 │
│ Update state with orders │
│ Re-render component      │
└──────────────────────────┘
```

### 3. Real-time Update Journey
```
┌─────────────────────┐
│ Orders Page Loaded  │
└──────┬──────────────┘
       │
       ▼
┌──────────────────────────┐
│ useSignalR().connect()   │
└──────┬───────────────────┘
       │ WebSocket /hubs/order
       │ + access_token=JWT
       ▼
┌──────────────────────────┐
│ Backend                  │
│ - Establish WebSocket    │
│ - Verify JWT             │
│ - Send "Connected" event │
└──────┬───────────────────┘
       │ Event "Connected"
       ▼
┌──────────────────────────┐
│ Client                   │
│ Listen for updates       │
│ .onOrderStatusChanged()  │
└──────────────────────────┘
       │
       │ (Server sends update)
       │ OrderStatusChanged event
       ▼
┌──────────────────────────┐
│ Frontend Handler         │
│ Update UI in real-time   │
│ No page refresh needed   │
└──────────────────────────┘
```

---

## Database Schema

```
┌──────────────────────────────┐
│        Users Table           │
├──────────────────────────────┤
│ Id (Guid, Primary Key)       │
│ UserName (string, Unique)    │
│ Email (string, Unique)       │
│ PasswordHash (string)        │
│ PhoneNumber (string, Null)   │
│ Type (UserType enum)         │
│  ├── Customer = 0            │
│  ├── ServiceProvider = 1     │
│  └── Admin = 2               │
│ IsActive (bool)              │
│ CreatedAt (DateTime)         │
│ LastLoginAt (DateTime, Null) │
└──────────────────────────────┘

Future Tables (Schema Ready):
├── Orders
├── Products
├── OrderItems
├── Payments
├── Reviews
└── Notifications
```

---

## Security Flow

```
┌─────────────────────────────────┐
│ 1. HTTPS/TLS                    │
│    All traffic encrypted        │
└─────────────────────────────────┘
                │
                ▼
┌─────────────────────────────────┐
│ 2. CORS Validation              │
│    Check origin header          │
│    Allow: localhost:3000,5173   │
└─────────────────────────────────┘
                │
                ▼
┌─────────────────────────────────┐
│ 3. Rate Limiting                │
│    100 req/min per IP           │
│    Prevent DDoS attacks         │
└─────────────────────────────────┘
                │
                ▼
┌─────────────────────────────────┐
│ 4. JWT Authentication           │
│    Validate token signature     │
│    Check expiration             │
│    Extract claims               │
└─────────────────────────────────┘
                │
                ▼
┌─────────────────────────────────┐
│ 5. Authorization Check          │
│    Verify user has permission   │
│    Check user type/role         │
└─────────────────────────────────┘
                │
                ▼
┌─────────────────────────────────┐
│ 6. SQL Injection Prevention      │
│    EF Core parameterized query  │
│    No string concatenation      │
└─────────────────────────────────┘
                │
                ▼
┌─────────────────────────────────┐
│ 7. Security Headers             │
│    X-Frame-Options: DENY        │
│    X-Content-Type: nosniff      │
│    Other headers configured     │
└─────────────────────────────────┘
```

---

## Deployment Architecture

```
                  ┌──────────────────┐
                  │   User Browser   │
                  └────────┬─────────┘
                           │
           ┌───────────────┼───────────────┐
           │               │               │
           ▼               ▼               ▼
      ┌────────┐    ┌─────────────┐  ┌────────┐
      │ CDN    │    │ LoadBalance │  │  DNS   │
      │(Fonts) │    │  r          │  │ Server │
      └────────┘    └─────────────┘  └────────┘
           │               │
           │       ┌───────┴────────┐
           │       │                │
           ▼       ▼                ▼
    ┌──────────────┐    ┌──────────────────┐
    │ App Server 1 │    │  App Server 2/3  │
    │ (API)        │    │  (Scaled API)    │
    └──────┬───────┘    └────────┬─────────┘
           │                     │
           │     ┌───────────────┘
           │     │
           ▼     ▼
    ┌───────────────────┐
    │  Database Server  │
    │  (SQL Server)     │
    │  with replicas    │
    └───────────────────┘
           │
           ▼
    ┌───────────────────┐
    │  Backup Storage   │
    │  (Daily backups)  │
    └───────────────────┘
```

---

## Development Environment

```
Developer Machine
│
├── Frontend Dev Server (Vite)
│   └── localhost:5173
│       └── Hot Module Replacement (HMR)
│
├── Backend Dev Server (.NET)
│   └── localhost:5001 (HTTPS)
│       └── Auto-reload on changes
│
├── SQL Server (Docker)
│   ├── localhost:1433
│   ├── Volume mounted for persistence
│   └── Health check configured
│
└── IDE/Editor (VS Code)
    ├── Frontend source
    ├── Backend source
    ├── Debugger attached
    └── Unit test runner
```

---

## Performance Optimizations

```
Frontend
├── Code Splitting (Vite)
├── Lazy Loading Components
├── Image Optimization
├── CSS Minification
├── JavaScript Minification
├── Gzip Compression
├── Offline Mode (No CDN)
└── Caching Strategy

Backend
├── Database Indexing
├── Connection Pooling
├── Response Compression
├── Middleware Caching
├── Rate Limiting
├── Async/Await Processing
├── SignalR Message Batching
└── Query Optimization
```

---

This architecture ensures:
✅ Scalability
✅ Maintainability
✅ Security
✅ Performance
✅ Real-time capabilities
✅ Offline functionality
✅ Clean code practices

---

**Reference:** See [INTEGRATION.md](./INTEGRATION.md) for complete setup guide.
